[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.7",
    "numba>=0.62.1",
    "scipy>=1.16.3",
]

[tool.uv.workspace]
members = [
    "pypy",
]
